# Example configuration for local development. Do NOT commit real secrets.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_local_db_password_here
DB_NAME=my_code

# JWT secret used to sign tokens. Replace with a strong secret in production via environment variables.
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Server port
PORT=3000

# Admin defaults (local only)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_local_admin_password_here

# Alternatively, set a single DATABASE_URL instead of DB_* fields:
# DATABASE_URL=mysql://root:your_local_db_password_here@localhost:3306/my_code
